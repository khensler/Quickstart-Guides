# Jekyll configuration for GitHub Pages
title: Linux Storage Configuration Guides
description: Best practices for NVMe-TCP and iSCSI storage on Linux distributions
baseurl: "/Quickstart-Guides"
url: "https://khensler.github.io"

# Build settings
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
  syntax_highlighter: rouge

# Theme - Minima (built-in GitHub Pages theme)
theme: minima

# Minima theme settings
minima:
  skin: classic

# Only show these pages in header (empty = none)
header_pages: []

# Analytics — GoatCounter site code (dashboard: https://<code>.goatcounter.com).
# Blank this out to disable analytics entirely; _includes/analytics.html emits
# nothing when it is unset.
goatcounter_code: khensler

plugins:
  - jekyll-seo-tag

# Include directories that Jekyll normally ignores
include:
  - common
  - distributions
  - Proxmox

# Exclude files from build
# CLAUDE.md documents Liquid syntax literally (including a bare `{% include %}`),
# which jekyll-optional-front-matter would otherwise try to render as a page.
# tests/ holds converter fixtures, not site content. They reference includes that
# only exist inside tests/fixtures/_includes/, so rendering them would be a hard
# build failure.
exclude:
  - LICENSE
  - Gemfile
  - Gemfile.lock
  - vendor
  - node_modules
  - CLAUDE.md
  - tests

# Default front matter
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

